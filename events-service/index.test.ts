import queueTrigger from './index';
import {defaultContext} from './tests/defaultContext'
import {request} from './tests/queueTriggerRequest.mock'

test('QueueTrigger should return --------', async () => {

  
// await queueTrigger(defaultContext.log(), JSON.stringify(request));
    expect((1)).toBe(1);
});
  